nacos:
  config: # 配置信息
    server-addr: localhost:8848
  discovery: #服务注册与发现
    server-addr: localhost:8848
management:
  endpoints:
    web:
      exposure:
        include: *
    health:
      show-details: always
